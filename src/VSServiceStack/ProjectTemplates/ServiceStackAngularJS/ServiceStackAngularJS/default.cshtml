@inherits ViewPage
@{
    ViewBag.Title = "Hello World Service";
}
<script>
    var app = angular.module('helloApp', []);

    app.controller('helloCtrl', ['$scope', '$http',
        function ($scope, $http) {
            $scope.$watch('name', function () {
                if ($scope.name) {
                    $http.get('/hello/' + $scope.name)
                        .success(function (response) {
                            $scope.helloResult = response.Result;
                            console.log(response.Result);
                        });
                }
            });
        }
    ]);
</script>
<div ng-app="helloApp">
    <div ng-controller="helloCtrl">
        <div>
            <label for="Name">Name</label>
            <input class="form-control input-sm" ng-model="name" id="Name" type="text" placeholder="Type your name">
            <span ng-bind="helloResult" ng-show="name"></span>
        </div>
    </div>
</div>
